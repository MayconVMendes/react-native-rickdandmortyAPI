import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    width: 250,
    height: 50,
    borderRadius: 5,
    borderWidth: 2,
    borderColor: 'black',
    paddingLeft: 10,
    paddingRight: 10,
    fontSize: 20,
  }
});
